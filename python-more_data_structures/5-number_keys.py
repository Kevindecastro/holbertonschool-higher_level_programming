def number_keys(a_dictionary):
    # Utiliser la fonction len(),
    # pour obtenir le nombre d'éléments (clés) dans le dictionnaire
    # Retourne le nombre total de clés dans le dictionnaire
    return len(a_dictionary)
