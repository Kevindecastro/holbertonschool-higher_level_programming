-- Sélectionne les colonnes score et name de la table second_table
-- et trient les résultats par score décroissant (du plus grand au plus petit)
SELECT score, name FROM second_table ORDER BY score DESC;
